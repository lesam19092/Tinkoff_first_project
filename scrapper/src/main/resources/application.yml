app:
  scheduler:
    enable: true
    interval: 5s
    force-check-delay: 5s

spring:
  application:
    name: scrapper

server:
  port: 1010

springdoc:
  swagger-ui:
    path: swagger-ui

logging:
  config: classpath:log4j2-plain.xml
